server:
  port: 8888
  servlet:
    context-path: /dome1
spring:
  datasource:
      url: jdbc:mysql://localhost:3306/user?characterEncoding=UTF-8
      username: root
      password: root
      driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#  profiles:
#    active: dev
